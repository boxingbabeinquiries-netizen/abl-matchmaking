const QUEUES = {
    RANKED: "ranked",
    RP: "rp"
};

const BUTTONS = {
    JOIN_RANKED: "join_ranked",
    LEAVE_QUEUE: "leave_queue"
};

const SELECTS = {
    PANEL_SELECT: "panel_select"
};

module.exports = {
    QUEUES,
    BUTTONS,
    SELECTS
};